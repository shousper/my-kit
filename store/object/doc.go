// Package object builds on top of the raw package stores by encoding and decoding the store data.
//
// This facilitates support for consumers of the store package to work with plain Go structs with
// a consistent storage format in the underlying store.
package object
